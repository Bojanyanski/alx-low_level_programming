#include "main.h"
#include <stdio.h>

/**
 * _isupper - a function that checks for uppercase letters
 * @c: the characterbto be tracked
 * Return: 1 if c is upper, 0 otherwise
 */
int _isupper(int c)
{
	return (c >=  'A' && c <= 'Z');
}
