#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @x - parameter1
 * @y - parameter2
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	return (x + y);
}
